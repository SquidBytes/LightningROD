"""Seed/demo data package — modular, contract-driven, time-shifting.

Replaces the legacy seed scripts (seed.py, seed_sample.py, seed_review_queue.py,
generate_sample_data.py). Activated by DEMO_MODE=true env var.

Modules: vehicle, locations, networks, evse_stalls, subscriptions,
charging_sessions, battery_status, trip_metrics, vehicle_status,
gps_location, gas_prices, review_queue.
"""
