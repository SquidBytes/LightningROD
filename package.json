{
  "name": "lightningrod",
  "version": "0.5.2",
  "description": "Self-hosted charging and vehicle analytics",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -i input.css -o web/static/css/output.css --minify",
    "build:css:watch": "tailwindcss -i input.css -o web/static/css/output.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4",
    "daisyui": "^5"
  },
  "dependencies": {
    "@playwright/test": "^1.61.1"
  }
}
